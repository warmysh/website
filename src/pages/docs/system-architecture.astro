---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Content, { getHeadings } from "../../../content/docs/system-architecture.md";

const tocHeadings = getHeadings().filter((heading) => heading.depth === 2 || heading.depth === 3);
---
<BaseLayout
  title="Архитектура системы"
  description="Архитектура контроллера муфельной печи PROFORGE: разделение интерфейсов, логики управления нагревом и силовой части, изоляция сети от прямого управления нагревом."
  tocHeadings={tocHeadings}
>
  <Content />
</BaseLayout>
