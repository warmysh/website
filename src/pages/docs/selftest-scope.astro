---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Content, { getHeadings } from "../../../content/docs/selftest-scope.md";

const tocHeadings = getHeadings().filter((heading) => heading.depth === 2 || heading.depth === 3);
---
<BaseLayout
  title="Область SELFTEST"
  description="Область самодиагностики SELFTEST контроллера муфельной печи PROFORGE: что проверяется перед разрешением нагрева и почему нагрев заблокирован до успешного завершения."
  tocHeadings={tocHeadings}
>
  <Content />
</BaseLayout>
