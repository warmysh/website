---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Content, { getHeadings } from "../../../content/docs/selftest-scope.md";

const tocHeadings = getHeadings().filter((heading) => heading.depth === 2 || heading.depth === 3);
---
<BaseLayout title="SELFTEST scope (public)" tocHeadings={tocHeadings}>
  <Content />
</BaseLayout>
